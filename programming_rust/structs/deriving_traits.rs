// NOTE: You can implement traits manually, for structs that only
// contain fields which already implement a trait, you can implement
// it automatically by deriving it.
#[derive(Copy, Clone, Debug, PartialEq)]
struct Point {
    x: f64,
    y: f64,
}

fn main() {}
