// `number` works similar to `boolean`

// Variables infer the type `number`
let a = 2;
// Constants infer a type literal
const b = 3;

// `bigint` also works like this
let c = 10n;
const d = 7n;

// NOTE: You can also explicitly specify the types.

