// `boolean` has the possible values `true`, `false`

// `boolean` is inferred for variables
let a = true;

// literal `false` is inferred for constants
const b = false;

// You can also use explicit types (both types and type literals)
let c: boolean = false;
let d: true = true;

