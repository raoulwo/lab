// An alternative to `#ifndef ... #endif` guards
// is the `#pragma once` directive which also prevents
// multiple header inclusions. The *pragma* isn't part
// of the C standard (it's however supported by most C
// preprocessors), so use the first version if
// portability is a concern.

#pragma once

// ...
